# Use the centralized Dockerfile for frontend
FROM node:20-alpine

# This is a reference Dockerfile that uses the centralized version
# See docker/Dockerfile.frontend for the actual implementation
# To build: docker build -f docker/Dockerfile.frontend -t todo-frontend .
# This file exists for convenience and standard project structure